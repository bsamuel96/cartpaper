# Bag Mockup Sources

The four licensed PSD source files are not present yet.

Expected files:

- `kraft-classic.psd`
- `white-premium.psd`
- `black-luxury.psd`
- `color-pop.psd`

Until these files are exported manually into clean runtime assets, the website uses labelled development placeholders from `public/mockups/`. Production verification must fail while placeholders remain.

Each real PSD should be exported into the V3 runtime set documented in `ASSET_PREPARATION.md`, including separate shadow, highlight, handle, print-mask, bag-color-mask, displacement-map, poster, and thumbnail files.
